# push_swap

A sorting algorithm project that sorts a stack using a limited set of operations.

## Features

- Stack manipulation
- Sorting algorithms
- Optimized operations
- Handles different input sizes

## Usage

Clone the repository and compile:

```bash
git clone <repository-url>
cd push_swap
make
